import { GenerateScheduleProps, GenerateScheduleResult } from './types';

function generateSchedule(props: GenerateScheduleProps): GenerateScheduleResult {
  return { classes: [{ name: '' }], teachers: [{ name: '' }] };
}

export { generateSchedule };
